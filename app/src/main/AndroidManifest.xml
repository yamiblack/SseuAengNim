<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jaktongdan.android.sseuaengnim">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SseuAengNim">
        <activity android:name=".ui.community.PostDetailActivity" />
        <activity android:name=".ui.community.AddBoardActivity" />
        <activity android:name=".ui.community.PostWriteActivity" />
        <activity android:name=".ui.planner.AddPlanActivity" />
        <activity android:name=".ui.myPage.PrivateSettingsActivity" />
        <activity android:name=".ui.community.PostListActivity" />
        <activity android:name=".ui.membership.JoinActivity" />
        <activity android:name=".ui.membership.LoginActivity" />
        <activity android:name=".ui.timer.TimerHistoryActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>